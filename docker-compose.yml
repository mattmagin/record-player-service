version: '3'
services:
  record-player-server:
    build: .
    image: record-player-service:latest
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
